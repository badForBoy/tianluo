// 背景图片
bg-image($url)
  background-image: url($url + "@2x.png")
  @media (-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3)
    background-image: url($url + "@3x.png")

// 不换行
no-wrap()
  text-overflow: ellipsis
  overflow: hidden
  white-space: nowrap

// 扩展点击区域
extend-click()
  position: relative
  &:before
    content: ''
    position: absolute
    top: -10px
    left: -10px
    right: -10px
    bottom: -10px

// 多行文本溢出显示省略号
line-wrap(n)
  display: -webkit-box
  -webkit-box-orient: vertical
  -webkit-line-clamp: n
  overflow: hidden

border-1px($color = #ccc, $radius = 0px)
  position: relative
  &::after
    content: ""
    pointer-events: none
    display: block
    position: absolute
    left: 0
    top: 0
    transform-origin: 0 0
    border-bottom: 1px solid $color
    border-radius: $radius
    box-sizing border-box
    width 100%
    height 100%
    @media (min-resolution: 2dppx)
      width: 200%
      height: 200%
      border-radius: $radius * 2
      transform: scale(.5) translateZ(0)
    @media (min-resolution: 3dppx)
      width: 300%
      height: 300%
      border-radius: $radius * 3
      transform: scale(.333) translateZ(0)

border-right($color = #ccc, $radius = 0px)
  position: relative
  &::after
    content: ""
    pointer-events: none
    display: block
    position: absolute
    left: 0
    top: 0
    transform-origin: 0 0
    border-right: 1px solid $color
    border-radius: $radius
    box-sizing border-box
    width 100%
    height 100%
    @media (min-resolution: 2dppx)
      width: 200%
      height: 200%
      border-radius: $radius
      transform: scale(.5) translateZ(0)
    @media (min-resolution: 3dppx)
      width: 300%
      height: 300%
      border-radius: $radius
      transform: scale(.333) translateZ(0)

border-top($color = #ccc, $radius = 0px)
  position: relative
  &::after
    content: ""
    pointer-events: none
    display: block
    position: absolute
    left: 0
    top: 0
    transform-origin: 0 0
    border-top: 1px solid $color
    border-radius: $radius
    box-sizing border-box
    width 100%
    height 100%
    @media (min-resolution: 2dppx)
      width: 200%
      height: 200%
      border-radius: $radius
      transform: scale(.5) translateZ(0)
    @media (min-resolution: 3dppx)
      width: 300%
      height: 300%
      border-radius: $radius
      transform: scale(.333) translateZ(0)

border-none()
  &::before
    display: none
  &::after
    display: none
